# プロジェクトメモ(重要事項の記録)

このファイルは、Claude Codeとの会話が長くなっても大事な決定事項を見失わないための
「重要事項メモ」です。新しい会話を始めるたびに、私(Claude)は自動でこれを読み込みます。

**⚠️ このファイルには実際のパスワード・APIキーなどの「値」そのものは書きません。**
「どこに設定されているか」だけを記録します。

---

## このリポジトリについて

- `reservation-app/` フォルダは古い試作版(プロトタイプ)です。
- **本番で実際に使っている予約アプリのファイルは、このリポジトリには入っていません。**
  - `Code_heartcreator.gs`(Google Apps Script バックエンド)
  - `個人用予約_完成版_テスト用.html`(heartcreator.jp/yoyaku/ に設置しているHTML)
  - これらの「本物」は、Google Apps Scriptエディタ本体と、Xserver上の本番環境そのものです。
  - Claude Codeとの作業中は一時フォルダ(セッション終了で消える)にコピーを置いて編集しているだけなので、
    **編集後は必ずGASエディタへの貼り付け・再デプロイと、Xserverへのアップロードまで完了させること。**

## 予約アプリの主な仕様(決定済み)

- カレンダー連携: オーナーのGoogleカレンダーの予定(手動追加分含む)と突き合わせて二重予約を防止。
  ただし予定のタイトルや詳細はお客様には一切見せない。
- 直前予約の受付制限: オーナー設定でリードタイム(例: 1時間/2時間/5時間)を設定可能。
- 銀行振込の場合: 予約時点では「仮予約」メールを送信し、入金確認後にオーナーが手動で
  「予約完了」に切り替えるとお客様へ本予約完了メールが送信される。
- インボイス(適格請求書)対応: オーナー設定「インボイス登録事業者：はい/いいえ」で
  登録番号(T+13桁)の表示有無を自動切り替え。PDF自動生成・自動送付。
- 通知メール:
  - 請求書発行時 → オーナーへ「請求書発行通知」
  - 入金確認・予約確定時 → オーナーへ「予約成立通知」
  - いずれも同じ操作で二重送信されないようガード済み。
- 二重予約警告の誤発火を修正済み: 同一顧客の再送信と、別顧客との本当の競合を区別するロジックを実装。
  ボタン連打防止(クライアント側)+ サーバー側の冪等性チェックの二重防御。
- reCAPTCHA v3導入済み: 新規予約フォームのスパム/不正利用対策。判定エラー時は
  「fail open」(お客様の予約を誤ってブロックしない)方針。

## 今後の方針(重要な合意事項)

- **秘密情報の空欄化(商品化用コピー作成)は「今」やらない。**
  reCAPTCHAキー・Zoom API情報・銀行口座・OWNER_EMAIL・事業者情報などを空欄にした
  「商品として配布する用のコピー」を作るのは、実際に商品化・配布を検討する段階になってから。
  今のライブ環境のファイルはそのまま実際の値を使い続ける。
- 在人予約の「場所」欄が空欄のときの表示崩れは、あえて直さない(サロン名を入れる運用で解決する方針のため)。

## 保留中のタスク

- [x] Zoom有料アカウントとのAPI連携: marketplace.zoom.us でServer-to-Server OAuthアプリを作成し、
      Account ID / Client ID / Client Secretを取得してGAS側の `ZOOM_ACCOUNT_ID` / `ZOOM_CLIENT_ID` /
      `ZOOM_CLIENT_SECRET` に設定済み。予約サイト側のオーナー設定「オンライン会議の設定」も
      「毎回、本物の新しいリンクを自動発行(Zoom API連携)」に切り替え・保存済み。
      Zoomスコープは以下3つを追加・有効化済み（会議の「作成」だけでなく「削除」「更新」は別スコープなので注意）：
      - `会議:書き込み:会議:管理者`（meeting:write:meeting:admin、作成用）
      - `会議:削除:会議:管理者`（meeting:delete:meeting:admin、キャンセル時の自動削除用）
      - `会議:更新:会議:管理者`（meeting:update:meeting:admin、日時変更時の自動更新用）
      新規予約時にZoom会議IDを台帳のJSON列（`zoomMeetingId`）に保存し、キャンセル時は`deleteZoomMeeting_`、
      日時変更時は`updateZoomMeetingTime_`で連携するよう実装済み（`getZoomMeetingId_`で台帳から引く）。
      実際に新規予約→キャンセルのテストで、Zoom会議が自動削除されることを確認済み（動作確認完了）。
- [ ] 商品化用コピーの作成(上記の通り、着手時期は未定・本人の意向待ち)。
- [ ] お客様向け説明書:
      - Zoom連携の設定ガイド(実体験ベース)を作成・公開済み:
        https://claude.ai/code/artifact/640150d8-5365-4200-8794-f48aee67c12f
      - 他の機能(週次営業時間、銀行振込〜入金確認の流れ、インボイス設定、キャンセル/変更等)の説明書は未作成。
      - 「フォルダごとお客様に渡し、お客様自身のClaude Codeに読み込ませて案内してもらう」という構想もあり
        (この場合はCLAUDE.mdを「私向けの技術メモ」ではなく「初見の人向けに自己完結した内容」に
        書き直す必要がある)。着手時期は未定、本人の意向待ち。
- [x] クレジットカード決済連携: **Stripeで実装・動作確認まで完了**。
      方式は「Stripe Checkout」（Stripeがホストする決済ページにリダイレクト）
      — カード情報は一切こちら側で扱わない、最も安全な標準的なやり方。
      流れ: 予約時 → GASがCheckout Sessionを作成 → お客様がStripeの決済画面で支払い →
      Stripeからのwebhook通知（`checkout.session.completed`）で自動的に予約確定・確認メール送信
      （銀行振込の「仮予約→入金確認」と同じ考え方だが、入金確認が自動になる）。
      支払い完了までは「カード決済待ち」の仮予約として枠を確保し、`CARD_HOLD_MINUTES`（60分）を
      過ぎても支払いが完了しなければ自動キャンセル。
      必要な秘密情報（今は空欄化しない方針）: `STRIPE_SECRET_KEY`（テストモードのキーで設定済み。
      本番運用時は `sk_live_...` に差し替えが必要）。
      **Webhook署名シークレット（STRIPE_WEBHOOK_SECRET）は使わない設計**——GASの`doPost`は
      リクエストヘッダーを読み取れず署名検証ができないため、代わりにwebhook受信時に必ずStripe API
      へ再照会して支払い状況を確認する方式（`fetchStripeCheckoutSession_`）にした。
      Stripe側のWebhookエンドポイント（ワークベンチ→Webhook→送信先を追加）は、GASのURLに対して
      `checkout.session.completed` イベントを送るよう設定済み（テストモード）。
      実装上の注意点（つまずいた点）:
      - GASはPOSTリクエストへの応答をブラウザ側で正しく読み取れない（応答を読もうとするとPOSTが
        GETに変換されてしまい`doGet`に着地する）。そのため決済ページ作成は
        POST（fire-and-forget, no-cors）で投げっぱなしにし、できあがったURLは別途
        `action=getCheckoutUrl`というGETでポーリングして取得する2段構えにしている。
      - 本番運用前に、STRIPE_SECRET_KEYを `sk_test_...` から `sk_live_...` に差し替え、
        Stripeダッシュボードの「本番環境」側でも同様にWebhookエンドポイントを追加する必要がある
        （テストモードと本番環境は別々に設定が必要）。
      PayPalも将来的に選択肢として追加予定（本人のPayPalビジネスアカウントは審査済み・使用可能）。
      本人の意向により、追加はまだ着手しない（まずはStripeの本番切り替えや他の作業を優先）。

## 学習に関する希望

- 今のheartcreator.jp予約アプリは、これまで通り「指示→実装」の共同作業スタイルで完成まで進める。
- それとは別に、HTML/CSS/JSの基礎を順番から学びたいという希望あり。
  別セッション(別の会話)でゼロから段階的に学ぶ形を検討中。今のプロジェクトの進行とは独立させる。
